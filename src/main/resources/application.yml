spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ateliertt
    username: atelier
    password: S3cret

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  springdoc:
    swagger-ui:
      path: /swagger-ui.html

cats:
  datasource-url: https://conseil.latelier.co/data/cats.json